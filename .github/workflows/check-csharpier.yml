name: CSharpier Check (noop)

on:
    pull_request:
    push:
        branches: [main, develop, workspace-*]

jobs:
    csharpier:
        name: CSharpier Check
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: Placeholder CSharpier check
              run: |
                  echo "CSharpier formatting check disabled → PASS (noop)"
                  exit 0
