name: No Direct Push to Main

on:
    push:
        branches: [main]

jobs:
    block:
        runs-on: ubuntu-latest
        steps:
            - name: Fail if this is a direct push to main
              run: |
                  echo "::error::Direct pushes to 'main' are not allowed. Open a PR."
                  exit 1
